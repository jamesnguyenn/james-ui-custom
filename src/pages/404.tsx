import ErrorPage from '@/components/ErrorPage'
import React from 'react'

interface Props { }

export default function ErrorCustomPage({ }: Props) {
    return (
        <ErrorPage />
    )
}

export const runtime = 'experimental-edge';